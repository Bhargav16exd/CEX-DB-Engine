generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Order {
  side           String
  type           String
  price          Int
  quantity       Int
  filledQuantity Int
  status         String
  userId         String
  deleted        Boolean  @default(false)
  createdAt      DateTime @default(now())
  orderId        String   @id
  market         String
  symbol         String

  @@index([userId])
}

model Stock {
  id      String  @id
  title   String
  symbol  String
  market  String
  deleted Boolean @default(false)
}

model User {
  id       String  @id
  username String  @unique
  password String
  balance  Int     @default(0)
  deleted  Boolean @default(false)
  role     String  @default("client")
}

model Fill {
  id           String   @id @default(uuid())
  makerID      String
  takerID      String
  makerOrderID String
  takerOrderID String
  quantity     Int
  market       String
  price        Int
  createdAt    DateTime @default(now())
  symbol       String
  makerSide    String
  takerSide    String

  @@index([createdAt])
  @@index([makerOrderID])
  @@index([symbol], map: "Fill_market_idx")
  @@index([takerOrderID])
}

model Contracts {
  id                String @id
  contract_quantity Int
  avg_price         Int
  collateral        Int
  realizedProfit    Int
  realizedLoss      Int
  stockSymbol       String
  userId            String
}
